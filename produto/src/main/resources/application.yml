eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8762

spring:
  application:
    name: produto
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:produto;MODE=PostgreSQL;
    username: user
    password: undefined
  jpa:
    hibernate:
      ddl-auto: update